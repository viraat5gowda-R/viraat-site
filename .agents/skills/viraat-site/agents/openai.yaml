interface:
  display_name: "Viraat Site"
  short_description: "Repo-specific patterns and workflows for viraat-site"
  default_prompt: "Use the viraat-site repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true